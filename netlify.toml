[build]
  command = "npm run build"
  publish = "dist"

# Nested demos — prefer static files; SPA fallback for Hearth client routes
[[redirects]]
  from = "/hearth/*"
  to = "/hearth/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
